# traffic_pulse

## Setup Instructions

### 1. Start the Backend
Open a terminal and run:
```bash
cd backend
npm install
npx nodemon server.ts
```

### 2. Start the Frontend
Open a second terminal and run:
```bash
cd frontend
npm install
npm run dev
```

Visit `http://localhost:5173` to see the application.

or you can now view on Github Pages
